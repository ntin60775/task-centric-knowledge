# План задачи TASK-2026-0015

## Правило

Для задачи существует только один файл плана: `plan.md`.
Если задача декомпозируется, каждая подзадача получает свой собственный `plan.md` внутри своей папки.

## Паспорт плана

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0015` |
| Parent ID | `—` |
| Версия плана | `1` |
| Связь с SDD | `не требуется` |
| Дата обновления | `2026-04-12` |

## Цель

Выполнить локальный refresh managed-файлов `task-centric-knowledge` в текущем репозитории и подтвердить консистентность результата обязательными проверками.

## Границы

### Входит

- создание task-контекста для работы;
- запуск installer в режимах `check` и `install`;
- анализ и верификация итогового diff.

### Не входит

- изменение логики skill-а;
- удалённая публикация результата;
- ручная переработка task-данных вне этой задачи.

## Планируемые изменения

### Код

- `нет`

### Конфигурация / схема данных / именуемые сущности

- возможная синхронизация managed-файлов knowledge-системы;
- синхронизация строки задачи в `knowledge/tasks/registry.md` через helper.

### Документация

- `task.md` и `plan.md` новой задачи;
- возможные обновления managed Markdown-файлов, если installer обнаружит drift.

## Зависимости и границы

### Новые runtime/package зависимости

- `нет`

### Изменения import/module-связей и зависимостей между модулями

- `нет`

### Границы, которые должны остаться изолированными

- `knowledge/tasks/registry.md` может получить только новую строку задачи и не должен быть перезаписан шаблоном skill-а;
- существующие каталоги задач и их артефакты не должны изменяться installer-ом.

### Критический функционал

- installer должен корректно обновить только managed-ресурсы текущего репозитория;
- helper должен синхронизировать branch и запись задачи без нарушения существующего реестра.

### Основной сценарий

- открыть новую задачу;
- создать task-ветку и строку в `registry.md`;
- выполнить installer;
- проверить diff и обязательные guards.

### Исходный наблюдаемый симптом

- `не требуется`

## Риски и зависимости

- installer действительно оказался идемпотентным по содержимому managed-файлов;
- итоговый diff ограничен артефактами новой задачи и строкой в `registry.md`.

## Связь с SDD

- `не требуется`

## Проверки

### Что можно проверить кодом или тестами

- `python3 /home/prog7/.agents/skills/task-centric-knowledge/scripts/install_skill.py --project-root /home/prog7/MyWorkspace/30-Knowledge/AI/ai-agents-rules-main --mode check`
- `python3 /home/prog7/.agents/skills/task-centric-knowledge/scripts/install_skill.py --project-root /home/prog7/MyWorkspace/30-Knowledge/AI/ai-agents-rules-main --mode install --force`
- `git diff --stat`
- `git diff --check`
- `scripts/check-docs-localization.sh knowledge/tasks/TASK-2026-0015-task-centric-knowledge-managed-refresh/task.md knowledge/tasks/TASK-2026-0015-task-centric-knowledge-managed-refresh/plan.md`

### Что остаётся на ручную проверку

- `не требуется`

## Шаги

- [x] Открыть knowledge-задачу и синхронизировать task-ветку.
- [x] Выполнить installer для текущего репозитория.
- [x] Проверить diff, `git diff --check` и локализацию изменённых Markdown-файлов.

## Критерии завершения

- refresh выполнен штатным installer-ом;
- итоговый diff понятен и ограничен ожидаемым набором файлов;
- обязательные проверки завершены без ошибок.
