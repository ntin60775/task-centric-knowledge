# Матрица верификации TASK-2026-0033

| Инвариант | Сценарий нарушения | Проверка/команда | Статус покрытия |
|-----------|--------------------|------------------|-----------------|
| `INV-01` | Repo-wide review находит medium/high defect, но задача закрывается без исправления или без доказательного снятия. | Первый review-pass от `Volta` и `Pasteur` зафиксировал четыре defect-а; повторный review-pass от тех же экспертов вернул clean verdict после fix-пакета. | covered |
| `INV-02` | Install/workflow/query/module/doctor/packaging surfaces после fix-pass выдают misleading diagnostics или regression. | `python3 -m unittest discover -s tests -v`; `python3 -m unittest tests.test_python_hardening_contracts tests.test_task_knowledge_cli tests.test_file_local_contracts_runtime tests.test_module_query_runtime -v`; `python3 scripts/task_knowledge_cli.py doctor --project-root /home/prog7/РабочееПространство/projects/PetProjects/task-centric-knowledge`; workflow error-path repro через `task_knowledge_cli.py --json workflow publish start ...`. | covered |
| `INV-03` | Исправления ломают синтаксис, импорт или общий regression-контур проекта. | `python3 -m compileall -q scripts tests`; `python3 -m unittest discover -s tests -v`; `/usr/bin/bash -lc 'for path in tests/test_*.py; do python3 -m unittest discover -s tests -p "$(basename "$path")" -v || exit 1; done'`. | covered |
| `INV-04` | Packaging/entrypoint слой требует сетевой bootstrap, побочный импорт-контекст или ломает local install contract. | `HOME="$(mktemp -d /tmp/tck-managed-home-XXXXXX)" make install-local USER_BIN="$(mktemp -d /tmp/tck-managed-bin-XXXXXX)"`; `task-knowledge --help`; `PYTHONPATH=scripts python3 -m task_knowledge --help`. | covered |
| `INV-05` | `task.md` / `plan.md` / `sdd.md` / `verification-matrix.md` расходятся с фактическими findings, verify и branch-state. | Финальная синхронизация task-артефактов, `python3 scripts/task_knowledge_cli.py task status --project-root /home/prog7/РабочееПространство/projects/PetProjects/task-centric-knowledge`, `python3 scripts/task_query.py --project-root /home/prog7/РабочееПространство/projects/PetProjects/task-centric-knowledge current-task --format json`, `bash scripts/check-docs-localization.sh knowledge/tasks/TASK-2026-0033-repo-wide-review-rerun knowledge/tasks/registry.md`, local finalize. | covered |
