# План задачи TASK-2026-0045

## Правило

Для задачи существует только один файл плана: `plan.md`.

## Паспорт плана

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0045` |
| Parent ID | `—` |
| Версия плана | `1` |
| Связь с SDD | `не требуется` |
| Дата обновления | `2026-05-06` |

## Цель

Разделить `README.md` на обзорный документ и `references/cli-reference.md` с полным CLI reference.

## Границы

### Входит

- Создание `references/cli-reference.md`.
- Сокращение `README.md`.
- Обновление перекрёстных ссылок.
- Проверка документации через `check-docs-localization.sh`.

### Не входит

- Изменение `SKILL.md`.
- Изменение других references.
- Изменение кода.

## Планируемые изменения

### Документация

- `README.md` — оставить: заголовок, краткое описание проекта, ссылки на `references/cli-reference.md`, `references/core-model.md`, `SKILL.md`, philosophy.
- `references/cli-reference.md` (новый) — перенести: поверхности установки, production rollout, глобальная установка, CLI layer, базовые команды, политика JSON, контракт потребительского runtime, владение репозиторием.

## Зависимости и границы

### Новые runtime/package зависимости

`нет`

### Изменения import/module-связей и зависимостей между модулями

`нет`

### Критический функционал

`не применимо` (только документация)

### Основной сценарий

1. Создать `references/cli-reference.md` с содержимым CLI-секций из `README.md`.
2. Сократить `README.md` до обзорного документа.
3. Добавить перекрёстные ссылки.
4. Проверить локализацию (`check-docs-localization.sh`).

### Исходный наблюдаемый симптом

`не требуется`

## Риски и зависимости

- **Внешние ссылки**: если кто-то ссылается на конкретные секции `README.md`, ссылки могут сломаться. Нужно добавить якоря или перенаправления.
- **`SKILL.md`**: если `SKILL.md` ссылается на секции `README.md`, эти ссылки нужно обновить.
- **`Makefile`**: если цели ссылаются на `README.md`, проверить.

## Связь с SDD

`не требуется`

## Проверки

### Что можно проверить кодом или тестами

- `make check` — source gate.
- `bash scripts/check-docs-localization.sh README.md references/cli-reference.md` — проверка локализации.
- `python3 -m unittest discover -s tests -v` — тесты не должны сломаться.

### Что остаётся на ручную проверку

- Визуальная проверка обоих документов.
- Проверка ссылок.

## Шаги

- [ ] Шаг 1: Изучить текущий `README.md` и выделить обзорные vs CLI-reference секции.
- [ ] Шаг 2: Создать `references/cli-reference.md` с CLI-контентом.
- [ ] Шаг 3: Сократить `README.md` до обзора (цель, философия, ссылки).
- [ ] Шаг 4: Добавить перекрёстные ссылки.
- [ ] Шаг 5: Проверить `SKILL.md` и другие файлы на ссылки на `README.md`.
- [ ] Шаг 6: Запустить `check-docs-localization.sh`.
- [ ] Шаг 7: Визуальная проверка.

## Критерии завершения

- `README.md` — обзорный документ.
- `references/cli-reference.md` — полный CLI reference.
- Ссылки корректны.
- `check-docs-localization.sh` проходит.
