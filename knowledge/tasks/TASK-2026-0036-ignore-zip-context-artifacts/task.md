# Карточка задачи TASK-2026-0036

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0036` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0036` |
| Технический ключ для новых именуемых сущностей | `ignore-zip-context-artifacts` |
| Краткое имя | `ignore-zip-context-artifacts` |
| Человекочитаемое описание | Исключить локальные артефакты `zip-context` из git-статуса standalone-репозитория. |
| Статус | `завершена` |
| Приоритет | `средний` |
| Ответственный | `Codex` |
| Ветка | `main` |
| Требуется SDD | `нет` |
| Статус SDD | `не требуется` |
| Ссылка на SDD | `—` |
| Дата создания | `2026-04-28` |
| Дата обновления | `2026-04-28` |

## Цель

Зафиксировать локальные ignore-правила для артефактов глобального навыка `zip-context`, чтобы временные файлы и zip-снимки не загрязняли рабочее дерево репозитория.

## Границы

### Входит

- добавить в `.gitignore` управляемый блок для `zip-context` артефактов;
- зафиксировать task-контур для конфигурационного изменения;
- проверить локализацию task-документов и базовый тестовый контур.

### Не входит

- изменение поведения самого навыка `zip-context`;
- удаление уже созданных локальных артефактов;
- расширение правил ignore на unrelated временные файлы.

## Контекст

- источник постановки: `гит джанитор`;
- исходный наблюдаемый симптом: в рабочем дереве появился config-diff `.gitignore` с правилами для `zip_context_ignore.md` и `output/share/*-zip-context-*.zip`;
- основной контекст сессии: текущий janitor-run.

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | `нет` |
| Конфигурация / схема данных / именуемые сущности | `.gitignore` получает правила для локальных артефактов `zip-context` |
| Интерфейсы / формы / страницы | `нет` |
| Интеграции / обмены | `нет` |
| Документация | `task.md`, `plan.md`, `registry.md` |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0036-ignore-zip-context-artifacts/`
- файл плана: `plan.md`
- связанные операции в `knowledge/operations/`, если есть: `нет`

## Контур публикации

| Unit ID | Назначение | Head | Base | Host | Тип публикации | Статус | URL | Merge commit | Cleanup |
|---------|------------|------|------|------|----------------|--------|-----|--------------|---------|
| `DU-01` | Локальная поставка ignore-правил для `zip-context` | `main` | `main` | `none` | `none` | `merged` | `—` | `—` | `выполнено` |

## Текущий этап

Задача завершена в janitor-run: ignore-правила для локальных `zip-context` артефактов зафиксированы, проверки пройдены, поставка подготовлена к публикации в `origin/main` и cleanup согласованных merged-веток.

## Стратегия проверки

### Покрывается кодом или тестами

- `bash scripts/check-docs-localization.sh`
- `python3 -m unittest discover -s tests`
- `task-knowledge task status --project-root /home/prog7/РабочееПространство/projects/PetProjects/task-centric-knowledge`
- `git diff --check`

### Остаётся на ручную проверку

- `не требуется`

## Критерии готовности

- `.gitignore` содержит только узкие правила для локальных `zip-context` артефактов;
- task-артефакты и `registry.md` синхронизированы с фактической веткой;
- проверки завершены без ошибок;
- результат влит в `main` и опубликован в `origin/main`;
- согласованные merged-ветки очищены.

## Итоговый список ручных проверок

- `не требуется`

## Итог

Задача завершена: `.gitignore` содержит узкий блок для локальных артефактов `zip-context`, task-контур синхронизирован, автоматические проверки пройдены.
