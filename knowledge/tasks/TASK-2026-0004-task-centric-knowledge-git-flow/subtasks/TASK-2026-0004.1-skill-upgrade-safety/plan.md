# План задачи TASK-2026-0004.1

## Правило

Для задачи существует только один файл плана: `plan.md`.

## Паспорт плана

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0004.1` |
| Parent ID | `TASK-2026-0004` |
| Версия плана | `1` |
| Связь с SDD | `не требуется` |
| Дата обновления | `2026-04-09` |

## Цель

Сделать обновление старой версии `task-centric-knowledge` на новую предсказуемым, безопасным и ясно фиксируемым в локальном git.

## Границы

### Входит

- rules/playbook для upgrade-перехода;
- проверки на отсутствие регрессий и потери task-данных;
- явное правило локального git-first и push only when remote exists.

### Не входит

- удалённый релизный процесс;
- автоматический push без запроса пользователя.

## Планируемые изменения

### Код

- При необходимости расширить helper read-only сигналами о наличии remote.
- Добавить тесты на idempotent update и сохранность существующих task-данных.

### Конфигурация / схема данных / именуемые сущности

- `нет`

### Документация

- Добавить upgrade-процедуру и git-transition правила в skill и managed-документы.

## Риски и зависимости

- Нельзя допустить перезапись project data при `--force`.
- Переходный commit должен быть отделён от последующих рабочих commit-ов, иначе момент перехода в git станет размытым.
- Предложение `push` без remote будет ложным сигналом и нарушит ожидаемый локальный workflow.

## Связь с SDD

- `не требуется`

## Проверки

### Что можно проверить кодом или тестами

- `python3 -m unittest skills-global/task-centric-knowledge/tests/test_install_skill.py`
- `python3 -m unittest skills-global/task-centric-knowledge/tests/test_task_workflow.py`
- `python3 ~/.agents/skills/markdown-localization-guard/scripts/markdown_localization_guard.py <измененные-md-файлы>`
- `git diff --check`

### Что остаётся на ручную проверку

- Практичность upgrade-playbook в реальном репозитории со старой версией skill-а.

## Шаги

- [x] Зафиксировать правила безопасного обновления старого навыка на новую версию.
- [x] Расширить тесты на idempotent upgrade и сохранность task-данных.
- [x] Добавить правило local git first и push only when remote exists.
- [x] Синхронизировать live-навык и зафиксировать переход отдельным commit-ом.

## Критерии завершения

- Обновление старого навыка на новую версию описано и проверяется.
- Момент перехода ясно выделяется в локальном git.
- Навык не предлагает `push`, если remote не обнаружен.
