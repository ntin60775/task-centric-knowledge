# Матрица верификации TASK-2026-0032

| Инвариант | Сценарий нарушения | Проверка/команда | Статус покрытия |
|-----------|--------------------|------------------|-----------------|
| `INV-01` | В diff попадают `.git`-артефакты архива, временные файлы или иной нерелевантный мусор вместо продуктовых изменений. | Экспертный diff-аудит архива по commit-tree; selective patch только по product-файлам; ручная сверка `git status --porcelain=v1` и итогового diff на task-ветке. | covered |
| `INV-02` | После первичной интеграции остаются medium/high findings от локального или экспертного review-pass. | Два независимых expert pass от субагентов; повторные локальные review-fix итерации; зелёные `tests.test_module_query_runtime`, `tests.test_file_local_contracts_runtime`, `tests.test_git_ops_runtime`, `tests.test_install_skill_governance`, `tests.test_task_workflow`. | covered |
| `INV-03` | Hardened changes ломают общий runtime/test контур или добавляют скрытую регрессию. | `python3 -m unittest discover -s tests -v`; `python3 -m compileall -q scripts tests`; `python3 -m unittest tests.test_cli_golden_contracts -v`; `python3 -m unittest tests.test_python_hardening_contracts -v`; smoke `PYTHONPATH=scripts python3 -m task_knowledge --help`; clean-venv `make install-local ...`, `task-knowledge --help`, `python -m task_knowledge --help`. | covered |
| `INV-04` | Task-артефакты расходятся с фактическим diff, findings и verify-результатом. | Синхронизация `task.md` / `plan.md` / `sdd.md` / `verification-matrix.md`; `bash scripts/check-docs-localization.sh knowledge/tasks/TASK-2026-0032-python-hardened-integration knowledge/tasks/registry.md`; `python3 scripts/task_knowledge_cli.py task status --project-root /home/prog7/РабочееПространство/projects/PetProjects/task-centric-knowledge`. | covered |
| `INV-05` | Работа остаётся только на task-ветке без local finalize/merge в `main`. | `python3 scripts/task_workflow.py --project-root /home/prog7/РабочееПространство/projects/PetProjects/task-centric-knowledge --task-dir knowledge/tasks/TASK-2026-0032-python-hardened-integration --finalize --base-branch main --commit-message "TASK-2026-0032: integrate python hardened snapshot"`; post-finalize `git status --porcelain=v1`; `git branch --show-current`; `git rev-list --left-right --count @{upstream}...HEAD`. | planned |
