# Карточка задачи TASK-2026-0015

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0015` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0015` |
| Технический ключ для новых именуемых сущностей | `—` |
| Краткое имя | `task-centric-knowledge-managed-refresh` |
| Человекочитаемое описание | `Локальный refresh managed-файлов task-centric-knowledge в текущем репозитории` |
| Статус | `завершена` |
| Приоритет | `средний` |
| Ответственный | `Codex` |
| Ветка | `task/task-2026-0015-task-centric-knowledge-managed-refresh` |
| Требуется SDD | `нет` |
| Статус SDD | `не требуется` |
| Ссылка на SDD | `—` |
| Дата создания | `2026-04-12` |
| Дата обновления | `2026-04-12` |

## Цель

Переустановить или обновить managed-файлы `task-centric-knowledge` в текущем репозитории через штатный installer, зафиксировать фактический diff и убедиться, что локальная knowledge-система остаётся консистентной после refresh.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Git-подсказка

- Поле `Ветка` хранит текущую активную ветку рабочего контекста, а не обязательную долгоживущую task-ветку.
- При открытии верхнеуровневой задачи стартовый контекст обычно синхронизируется в `task/<task-id-lower>-<slug>`.
- Для первой и последующих поставок helper может переводить `Ветка` в delivery-ветку вида `du/<task-id-lower>-uNN-<slug>`.
- Для подзадачи по умолчанию можно указывать ветку родителя, если отдельная ветка или delivery unit не нужны.
- При каждом создании или переключении ветки нужно синхронизировать это поле и строку в `registry.md`.

## Границы

### Входит

- открыть отдельную knowledge-задачу под refresh managed-файлов;
- выполнить `install_skill.py` для текущего репозитория;
- зафиксировать изменения в `knowledge/` и `AGENTS.md`, если installer их внесёт;
- проверить итоговый diff и базовые инварианты knowledge-системы.

### Не входит

- переработка логики installer или helper вне объёма фактического refresh;
- изменение пользовательских task-данных без прямой необходимости;
- удалённая публикация, `push` и PR/MR.

## Контекст

- источник постановки: выбор пользователя выполнить обновление или переустановку managed-файлов skill-а в текущем репозитории;
- связанная бизнес-область: локальная knowledge-система и managed-дистрибутив `task-centric-knowledge`;
- ограничения и зависимости: `knowledge/tasks/registry.md` считается project data и не должен перезаписываться дистрибутивом;
- исходный наблюдаемый симптом / лог-маркер: `не требуется`
- основной контекст сессии: `текущая задача`

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | Используется штатный installer `task-centric-knowledge` без изменения его логики |
| Конфигурация / схема данных / именуемые сущности | Возможен refresh managed-структуры `knowledge/` и managed-блока `AGENTS.md` |
| Интерфейсы / формы / страницы | `нет` |
| Интеграции / обмены | `нет` |
| Документация | `task.md`, `plan.md` этой задачи и возможные обновления managed Markdown-файлов knowledge-системы |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0015-task-centric-knowledge-managed-refresh/`
- файл плана: `plan.md`
- файл SDD: `—`
- пользовательские материалы: сообщения пользователя от `2026-04-12`
- связанные коммиты / PR / ветки: `task/task-2026-0015-task-centric-knowledge-managed-refresh`
- связанные операции в `knowledge/operations/`, если есть: `нет`

## Контур публикации

Delivery unit для локального refresh пока не требуется.

| Unit ID | Назначение | Head | Base | Host | Тип публикации | Статус | URL | Merge commit | Cleanup |
|---------|------------|------|------|------|----------------|--------|-----|--------------|---------|
| `—` | — | `—` | `—` | `none` | `none` | `planned` | `—` | `—` | `не требуется` |

## Текущий этап

Задача завершена: installer выполнен с `--force`, после чего git-зафиксированный diff остался идемпотентным для managed-файлов дистрибутива.

## Стратегия проверки

### Покрывается кодом или тестами

- `python3 /home/prog7/.agents/skills/task-centric-knowledge/scripts/install_skill.py --project-root /home/prog7/MyWorkspace/30-Knowledge/AI/ai-agents-rules-main --mode check`
- `python3 /home/prog7/.agents/skills/task-centric-knowledge/scripts/install_skill.py --project-root /home/prog7/MyWorkspace/30-Knowledge/AI/ai-agents-rules-main --mode install --force`
- `git diff --stat`
- `git diff --check`
- `scripts/check-docs-localization.sh knowledge/tasks/TASK-2026-0015-task-centric-knowledge-managed-refresh/task.md knowledge/tasks/TASK-2026-0015-task-centric-knowledge-managed-refresh/plan.md`

### Остаётся на ручную проверку

- `не требуется`

## Критерии готовности

- задача оформлена в knowledge-системе и синхронизирована с task-веткой;
- installer выполнен штатной командой `--mode install --force` для текущего репозитория;
- git-зафиксированный diff ограничен новой задачей и строкой в `registry.md`;
- обязательные проверки и локализационный guard выполнены.

## Итоговый список ручных проверок

- `не требуется`

## Итог

Открыта и синхронизирована задача `TASK-2026-0015`, создана ветка `task/task-2026-0015-task-centric-knowledge-managed-refresh`, после чего выполнен штатный installer `task-centric-knowledge` в режиме `--mode install --force`.

Installer подтвердил совместимое состояние и формально обновил managed-ресурсы из дистрибутива, но git-зафиксированного контентного diff по `AGENTS.md` и файлам `knowledge/` не появилось. Это означает, что текущий репозиторий уже был синхронизирован с актуальным managed-снимком skill-а, а refresh отработал идемпотентно.

Project data сохранены: `knowledge/tasks/registry.md` не перезаписывался шаблоном, а получил только новую строку задачи через helper.
